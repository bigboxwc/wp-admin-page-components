/**
 * WordPress dependencies.
 */
import { render } from '@wordpress/element';

/**
 * Internal dependencies
 */
import Layout from './layout';

render( <Layout />, document.getElementById( 'wp-admin-page-components' ) );
